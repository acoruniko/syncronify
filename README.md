# Syncronify
Proyecto Django inspirado en Spotify UI. Automatiza la sincronizacion mediante tareas.

## Instalación
1. Clonar el repo
2. Crear venv y activar
3. Instalar dependencias: `pip install -r requirements.txt`
4. Ejecutar: `python manage.py runserver`